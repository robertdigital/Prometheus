export class Executor{
    /**
     * Will take a decision from the evaluator and place the appropriate order on coinbase
     *
     * @returns
     * @memberof Executor
     */
    public async executeDecision(){
        return true;
    }
}